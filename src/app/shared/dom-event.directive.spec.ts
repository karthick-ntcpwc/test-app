import { DomEventDirective } from './dom-event.directive';

describe('DomEventDirective', () => {
  it('should create an instance', () => {
    const directive = new DomEventDirective();
    expect(directive).toBeTruthy();
  });
});
